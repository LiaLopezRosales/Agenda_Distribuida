# Copy this file to deploy/stack.env and replace the placeholders before running
# docker stack deploy -c deploy/docker-stack.yml agenda
CLUSTER_HMAC_SECRET=replace-me-with-long-random-string
AUDIT_API_TOKEN=replace-me-with-admin-token
DISCOVERY_SEEDS=http://manager-1:8080,http://manager-2:8080
LOG_FORMAT=json


